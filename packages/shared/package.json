{
  "name": "@caspilot/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/canonical.ts",
  "types": "./src/canonical.ts",
  "exports": {
    ".": {
      "import": "./src/canonical.ts",
      "types": "./src/canonical.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@caspilot/tsconfig": "workspace:*",
    "@types/node": "^22",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
